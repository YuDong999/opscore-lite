# OpsCore Lite 演进路线

> 定位：面向运维/SRE 的轻量多机运维控制台

## 当前状态

- 单二进制 35M，内存 35-55M，无外部依赖
- 8 大模块：系统资源 / 服务发现 / 防火墙和网络 / 系统诊断 / 任务与存储 / Ansible 多机管理 / 插件中心 / 容器管理
- 20 发行版能力探测 + 命令路由（新增发行版边际成本 = `inventory.go` 加 1 行默认值）
- SSH / Agent 双路径，原生多机管理
- 前端 React + ECharts，SPA 单页应用

## 已知不足与改进项

### P0 - 核心缺失

| 短板 | 运维影响 | 改进方向 |
|---|---|---|
| 无监控告警 | 只能被动看，无法主动发现异常 | CPU / 内存 / 磁盘阈值 + 通知推送 |
| 测试覆盖率低 | 生产部署信心不足 | 补写 handlers 单元测试，目标核心路径 80%+ |

### P1 - 架构债务

| 问题 | 风险 | 改进方向 |
|---|---|---|
| `buildFirewallCommand` 700+ 行 | 新增规则类型需改大函数，易出错 | 拆分为 backend-specific builder |
| netconfig 远程 shell 拼接 | 转义 / 注入风险，难测试 | 参数化命令构造，本地远程统一执行器 |
| 本机 / 远程双份实现仍有残留 | 维护成本翻倍，bug 修复需改两处 | 持续提取公共执行器 + 公共解析函数 |

### P2 - 体验优化

| 问题 | 运维影响 | 改进方向 |
|---|---|---|
| 无主机分组 / 标签视图 | 50+ 主机难以管理 | 前端增加分组 / 标签筛选 + 批量操作 |
| 软件包管理简陋 | 不如宝塔直观 | 可更新列表 + 批量安装 + 回滚 |
| 无历史趋势图表 | 无法回溯故障时间线 | 时序数据存储 + 历史曲线 |

### P3 - 未来探索

| 方向 | 说明 |
|---|---|
| 监控历史趋势 | 时序数据存储 + 历史图表 |
| 用户权限隔离 | 多账号 + 主机权限控制 |
| 备份 / 快照管理 | Timeshift / Btrfs 快照集成 |
| 告警渠道 | 企业微信 / 钉钉 / 邮件 / Webhook |
| 主机标签与批量执行 | 按标签筛选主机 + 批量执行命令 |

## 非目标

- 不做软件商店 / 一键部署（偏离运维核心定位）
- 不做网站 / 数据库管理（与宝塔差异化）
- 不做桌面 GUI（保持纯 Web + CLI 运维场景）
